# our main test script
services: docker
language: ruby
script:
  - docker run -u root --rm -w /workarea -v $PWD:/workarea/src/xAODAnaHelpers atlas/analysisbase:${ABV_CM} /workarea/src/xAODAnaHelpers/ci/run.sh
env:
  - ABV_CM=21.2.3
  - ABV_CM=21.2.4

# below are specific matrix includes for building documentation
#    and running cppcheck (not actually used)
matrix:
  include:
    - language: python
      python: 2.7
      sudo: false
      env: JOB="Building online documentation"
      addons:
        apt:
          packages:
            - doxygen
            - doxygen-doc
            - doxygen-gui
            - doxygen-latex
            - graphviz
      install: pip install -r docs/requirements.txt
      script:
        - cd docs
        - doxygen
        - make html
        - cd ..
        - touch docs/_build/html/.nojekyll
      deploy:
        provider: pages
        skip_cleanup: true
        github_token: $GITHUB_TOKEN
        local_dir: docs/_build/html
        on:
          branch: master

    - language: cpp
      sudo: required
      env: JOB="uncrustify check"
      before_script:
        - sudo apt-get -qq update
        #  - sudo apt-get install -y -qq uncrustify cppcheck
        - sudo apt-get install -y -qq cppcheck
      # must install 0.64 of uncrustify, a later one
      install:
        - wget https://github.com/uncrustify/uncrustify/archive/uncrustify-0.64.tar.gz
        - tar -xzvf uncrustify-0.64.tar.gz
        - mkdir build-uncrustify
        - cd build-uncrustify
        - cmake ../uncrustify-uncrustify-0.64
        - make
        - mv uncrustify ../uncrustify
        - cd ../
        - rm -rf build-uncrustify
      script:
        - ./uncrustify -c uncrustify.cfg --check **/**.cxx || exit 0

notifications:
  email:
    on_success: never
    on_failure: always
